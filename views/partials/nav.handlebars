<!-- Navigation -->
<nav class="navbar">
  <div class="container-fluid navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Aemo</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
            <li>
                <a class="page-scroll" href="/profile/">My Profile</a>
            </li>
            <li>
                <a href="/users/sign-out" class="page-scroll" data-toggle="modal">Logout</a>
            </li>
        {{else}}
            <li>
                <a class="page-scroll" data-toggle="modal" data-target="#login-modal">Login/Sign Up</a>
            </li>
        {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
    <footer class="row">
        <div class="col-md-12 navFooter">
        </div>
    </footer>
  </div><!-- /.container-fluid -->
</nav>